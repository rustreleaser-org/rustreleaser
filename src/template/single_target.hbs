# typed: false
# frozen_string_literal: true

# DO NOT EDIT THIS FILE

class {{ name }} < Formula
    {{ #if description }}
    desc "{{ description }}"
    {{ /if }}
    {{ #if homepage }}
    homepage "{{ homepage }}"
    {{ /if }}
    version "{{ version }}"
    {{ #if license }}
    license "{{ license }}"
    {{ /if }}
    url "{{ targets.0.Single.url }}"
    hash "{{ targets.0.Single.hash }}"

    def install
        {{{ ../../install_info }}}
    end
    {{ #if caveats}}

    def caveats
        "{{ caveats }}"
    end
    {{ /if }}
    {{ #if test }}

    test do
        {{ test }}
    end
    {{ /if }}
end
